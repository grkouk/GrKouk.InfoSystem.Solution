@page
@model GrKouk.WebRazor.Pages.CommonEntities.ClientProfiles.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClientProfile[0].Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClientProfile[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClientProfile[0].Serial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClientProfile[0].Data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClientProfile[0].Company)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ClientProfile) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Serial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Company.Code)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
